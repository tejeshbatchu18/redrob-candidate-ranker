# --- Core (required; rank.py runs end-to-end with just these, no network) ---
numpy>=1.24
scikit-learn>=1.3
rank-bm25>=0.2.2

# --- Optional: best-quality semantic path (run precompute.py once, offline) ---
# Install these to enable precomputed sentence-transformer + FAISS embeddings.
# rank.py automatically falls back to TF-IDF if they are absent.
# sentence-transformers>=2.2
# faiss-cpu>=1.7
